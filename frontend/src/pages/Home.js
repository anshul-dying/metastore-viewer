import React from "react";
import MetadataViewer from "../components/MetadataViewer";

const Home = () => {
    return (
        <div>
            <MetadataViewer />
        </div>
    );
};

export default Home;
